# BOSS Zhipin Auto-Reply Chrome Extension

A Chrome MV3 extension that automates candidate replies on [BOSS Zhipin](https://www.zhipin.com) (the largest Chinese-language professional recruitment platform). Click a button in the extension popup and the extension will scan your unread chats, generate a context-aware response for each candidate, and send it through the BOSS web UI — without leaving the page.

## ✨ Features

### 🎯 Core
- **One-click auto-reply loop** — flip the toggle in the popup and the extension starts replying to unread conversations. The loop self-chains with human-like jitter between messages and stops the moment you turn it off.
- **Context-aware replies** — each reply is generated by an LLM that sees the candidate's name, the job title they applied for, and the latest message in the conversation.
- **Three-option LLM output** — the prompt asks the model for three short reply candidates, separated by `{option1：…, option2：…, option3：…}`. The extension parses that custom format, picks one at random, and sends it. (Default endpoint is the internal `ai.dadaex.cn` proxy; the LLM in use is configurable in `src/shared/prompt.ts`.)
- **Daily limit guard** — the loop auto-disables when `sent >= dailyLimit` (default 200/day). The popup shows an amber "Daily limit reached" state and a warning toast, and re-enables itself at midnight when the daily counter is reset.

### 🛡️ Safety
- **LRU replied cache** — the extension remembers which conversations it has already replied to (up to 2,000 entries) so it never sends the same canned response twice in a row.
- **Race-guard lock** — only one loop tick runs at a time. If the service worker is killed mid-loop, the lock is released on the next SW startup.
- **Per-tab find** — the loop targets a single BOSS chat tab; if none is open, it no-ops silently.
- **URL-pattern match** — Chrome match pattern `https://www.zhipin.com/web/chat*` (scheme explicit, host wildcard scoped).

### 🖼️ Popup UI
- **280 px layout** with title, toggle row, 2×2 status grid (sent/limit, errors, running), and an error line.
- **Polling** the service worker once per second while the popup is open.
- **Toast notifications** for state transitions (enabled, disabled, limit reached).
- **Warning button state** when the daily limit is hit — the toggle is disabled, the label changes to "Daily limit reached", and the row turns amber.

### 🔧 Developer Tools
- **`tools/inspect-boss.cjs`** — a Node script that uses Chrome DevTools Protocol to dump the live DOM of the BOSS chat page. Produces `tools/inspect-out/document.html`, `first-row.html`, `message-pane.html`, and `probe-report.json` so you can keep `dom.ts` selectors in sync as BOSS rolls new builds.

## 📋 Prerequisites

- **Node.js 20+**
- **Google Chrome** (or any Chromium-based browser with manifest v3 support)
- A **BOSS Zhipin** account logged in via Chrome (so the chat page can fetch your real conversations)

## 🚀 Quick Start

### 1. Install dependencies

```bash
npm install
```

### 2. Set up the LLM endpoint

The default endpoint is the internal proxy at `http://ai.dadaex.cn/backapi/chatGpt/chatAll`. CORS for that host is already whitelisted in `manifest.config.ts` via `host_permissions`.

If you want to use a different endpoint (e.g. Google Gemini directly), edit [src/background/gemini.ts](src/background/gemini.ts) and remove the `ai.dadaex.cn` host from `manifest.config.ts`.

### 3. Start the dev server

```bash
npm run dev
```

Vite will build the bundle into `dist/`.

### 4. Load the extension into Chrome

1. Open `chrome://extensions/`
2. Enable **Developer mode** (top right)
3. Click **Load unpacked** and select the `dist/` directory
4. The BOSS ZHIPIN icon should appear in your toolbar

### 5. Use the extension

1. Open `https://www.zhipin.com/web/chat/index` in Chrome (log in if needed)
2. Click the extension icon → click **Enable**
3. The popup polls the service worker once a second. The status grid updates as replies are sent.
4. Click **Disable** to stop the loop at any time.

## 🛠️ Build for production

```bash
npm run build
```

This runs `tsc -b && vite build` and produces a release artifact at `release/boss-zhipin-extension-<version>.zip` (auto-generated by `vite-plugin-zip-pack`).

## 🧪 Update selectors after a BOSS redesign

BOSS rolls new builds regularly, and class names can change. To re-verify the selectors:

1. Start a Chrome instance with the remote-debugging port:

   ```powershell
   # PowerShell
   Start-Process "C:\Program Files\Google\Chrome\Application\chrome.exe" `
     -ArgumentList "--remote-debugging-port=9222", `
     "--user-data-dir=C:\tmp\chrome-debug-profile"
   ```

2. In that Chrome, open `https://www.zhipin.com/web/chat/index` and log in
3. Open any conversation row so the right panel renders
4. Run:

   ```bash
   node tools/inspect-boss.cjs
   ```

5. Inspect `tools/inspect-out/probe-report.json` to see which selector candidates hit / miss
6. Update [src/content/features/auto-reply/dom.ts](src/content/features/auto-reply/dom.ts) with the new selectors
7. `npm run build` and reload the extension

## 📁 Project Structure

```
src/
├── background/                 # Service worker (orchestrator)
│   ├── index.ts               # message handler, state machine
│   ├── loop.ts                # one tick of the auto-reply loop
│   ├── scheduler.ts           # jitter helper (only)
│   ├── gemini.ts              # LLM call to internal proxy
│   └── storage.ts             # chrome.storage wrapper, LRU replied cache
│
├── content/
│   └── features/auto-reply/   # Content script injected into BOSS pages
│       ├── index.ts           # message router
│       ├── dom.ts             # selectors, DOM helpers
│       ├── scrape.ts          # findUnread, openConv, readLastCandidateMessage
│       ├── act.ts             # typeAndSend (composer)
│       └── wait.ts            # polling helpers
│
├── shared/                     # shared types
│   ├── messages.ts            # message union for SW <-> popup / content
│   ├── prompt.ts              # SYSTEM_PROMPT, FALLBACK_REPLY
│   └── types.ts               # AppConfig, Conversation, Persisted
│
├── popup/                      # Extension popup UI
│   ├── App.tsx                # main view
│   ├── main.tsx               # React root
│   ├── scripts.ts             # toast helper
│   ├── index.html
│   └── index.css              # @tailwind directives
│
└── sidepanel/                  # (scaffolded, not wired)

manifest.config.ts              # MV3 manifest in TypeScript
tools/inspect-boss.cjs          # CDP DOM dump tool
tools/inspect-out/              # generated by inspect-boss.cjs
docs.md                         # daily log of all user prompts
```

## 🔌 Message protocol

The service worker, content script, and popup communicate through typed message unions defined in [src/shared/messages.ts](src/shared/messages.ts).

**Popup → SW (`PopupToSw`):**
- `GET_STATE` — returns current `STATE` (enabled, sent, dailyLimit, errors, lastErrorMsg, isRunning, reachedDailyLimit)
- `TOGGLE_ENABLED` `{ enabled: boolean }` — flips the global toggle; when enabling, kicks off `runOnce()` immediately
- `UPDATE_CONFIG` `{ config }` — currently a no-op (API key is hardcoded)
- `CLEAR_REPLIED` — clears the LRU replied cache
- `RUN_ONCE` — fire-and-forget single tick (debug)

**SW → Content (`SwToContent`):**
- `PING` — health check
- `SCRAPE_UNREAD` — return the list of unread conversations
- `OPEN_CONV` `{ convId }` — click the row, wait for the right panel to render
- `READ_LAST_MESSAGE` `{ convId }` — return the last bubble's text from the right pane
- `SEND_REPLY` `{ convId, text }` — type into the composer and click Send
- `TEST_SELECTORS` — run a probe for every entry in `SEL`

## 🐛 Troubleshooting

| Symptom | Likely cause | Fix |
|---|---|---|
| `Could not establish connection. Receiving end does not exist` | Content script not injected on the BOSS tab | Reload the extension in `chrome://extensions/`, then press F5 on the BOSS tab |
| Empty string from `READ_LAST_MESSAGE` | React render raced the call, or the last bubble is a system card | Already handled with a 3 s retry + system-bubble filter in `readLastCandidateMessage` |
| `Access-Control-Allow-Origin` error on the LLM endpoint | Missing `host_permissions` for the LLM host | Add the host to `manifest.config.ts > host_permissions` |
| `toolResult` from AskUserQuestion never returns | `chrome.runtime.onMessage` listener returns `false` instead of `true` | Make sure the listener returns `true` to keep the channel open for `sendResponse` |
| `Error: Cannot find name 'return0'` (Tailwind v4 source scanner bug) | Tailwind v4's source scanner crashes on certain Unicode | Already mitigated: emoji in `App.tsx` are stored as `&#128236;` HTML entities |

## 📜 License

Internal tool for eptrade.cn. Not for public release.
